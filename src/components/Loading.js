import React from "react";

const Loading = () => {
  return (
    <div className="h-screen flex justify-center items-center">
      <p className="text-xl text-red-700">Loading....</p>
    </div>
  );
};

export default Loading;
